# Frontend (React)

This directory will contain the React frontend application.

## Setup
```bash
npx create-react-app .
# or
npx create-vite@latest . --template react
npm install
npm start
```

> **Note:** Nikhil will initialize the React app here.
