# 🍽️ Yelp Prototype — Lab 1

> **Course:** Distributed Systems · **Due:** March 24, 2026 · 11:59 PM · **Points:** 40

A full-stack **Yelp-style restaurant discovery and review platform** with an **AI-powered chatbot assistant** that provides personalized restaurant recommendations based on user preferences and natural language queries.

---

## 🧑‍💻 Team

| Member | Role | Responsibilities |
|---|---|---|
| **Prakhar Singh** | Backend Lead | FastAPI APIs, MySQL schema, JWT auth, AI chatbot service (Langchain + Tavily), API docs |
| **Nikhil Khaneja** | Frontend Lead | React UI, all pages/components, chat interface, responsive styling (TailwindCSS) |

---

## 🛠️ Tech Stack

| Layer | Technology |
|---|---|
| **Frontend** | React 18, Vite, TailwindCSS, Axios |
| **Backend** | Python 3.9+, FastAPI |
| **Database** | MySQL 8.0+ |
| **AI Chatbot** | Langchain, Google Gemini (via langchain-google-genai), Tavily Web Search |
| **Authentication** | JWT (python-jose) + bcrypt (passlib) |
| **API Docs** | Swagger UI (auto-generated by FastAPI) |

---

## 📁 Project Structure

```
yelp-prototype-lab1/
├── backend/                    # Python FastAPI backend
│   ├── main.py                 # App entry — registers all routers, CORS, static files
│   ├── db/
│   │   ├── database.py         # SQLAlchemy engine + session dependency
│   │   └── seed.py             # Seed script (sample users, restaurants, reviews)
│   ├── models/
│   │   ├── models.py           # ORM models (User, Restaurant, Review, Photo, Favorite, UserPreference)
│   │   └── schemas.py          # Pydantic request/response schemas
│   ├── routes/
│   │   ├── auth.py             # POST /auth/signup, /auth/login
│   │   ├── users.py            # Profile CRUD + photo upload + preferences
│   │   ├── restaurants.py      # Restaurant CRUD + search/filter/pagination + photos
│   │   ├── reviews.py          # Review CRUD (own reviews only) + auto rating recalc
│   │   ├── favorites.py        # Add/remove/list favorites
│   │   ├── history.py          # GET /users/me/history
│   │   └── ai_chat.py          # POST /ai-assistant/chat (Gemini + Tavily)
│   ├── services/
│   │   └── auth.py             # JWT creation/verification + bcrypt hashing
│   ├── requirements.txt
│   ├── .env.example
│   └── uploads/                # Uploaded images (auto-created, gitignored)
│
├── frontend/                   # React (Vite) frontend
│   ├── src/
│   │   ├── components/
│   │   │   ├── Navbar.jsx      # Navigation bar
│   │   │   ├── ProtectedRoute.jsx  # Auth guard for routes
│   │   │   ├── RestaurantCard.jsx  # Reusable restaurant card
│   │   │   └── StarRating.jsx  # Interactive star rating component
│   │   ├── pages/
│   │   │   ├── LoginPage.jsx
│   │   │   ├── SignupPage.jsx
│   │   │   ├── ExplorePage.jsx         # Main search/discovery page
│   │   │   ├── RestaurantDetailsPage.jsx
│   │   │   ├── AddRestaurantPage.jsx
│   │   │   ├── ProfilePage.jsx         # Profile + preferences editor
│   │   │   ├── ChatPage.jsx            # AI chatbot interface
│   │   │   ├── FavoritesPage.jsx
│   │   │   └── HistoryPage.jsx
│   │   ├── services/
│   │   │   ├── api.js              # Axios instance with JWT interceptor
│   │   │   ├── authService.js      # Auth API calls
│   │   │   ├── restaurantService.js # Restaurant + review API calls
│   │   │   ├── userService.js      # Profile + preferences API calls
│   │   │   └── aiService.js        # AI chatbot API calls
│   │   ├── App.jsx             # Router + layout
│   │   ├── main.jsx            # App entry
│   │   └── index.css           # TailwindCSS styles
│   ├── index.html
│   ├── vite.config.js
│   ├── tailwind.config.js
│   ├── postcss.config.js
│   ├── package.json
│   └── .env.example
│
├── PROJECT_BOARD.md            # Detailed task tracker with status
├── Lab1-Yelp.pdf               # Lab requirements document
└── README.md                   # ← You are here
```

---

## 🚀 Getting Started

### Prerequisites

| Tool | Version | Check |
|---|---|---|
| Python | 3.9+ | `python3 --version` |
| Node.js | 18+ | `node --version` |
| MySQL | 8.0+ | `mysql --version` |
| npm | 9+ | `npm --version` |

### 1. Clone the Repository

```bash
git clone https://github.com/prakharsinghpersonal/yelp-prototype-lab1.git
cd yelp-prototype-lab1
```

### 2. Database Setup

```bash
# Connect to MySQL and create the database
mysql -u root -e "CREATE DATABASE IF NOT EXISTS yelp_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
```

> The tables are auto-created by SQLAlchemy ORM when the backend starts, or you can run the seed script.

### 3. Backend Setup

```bash
cd backend

# Create and activate virtual environment
python3 -m venv venv
source venv/bin/activate        # macOS/Linux
# venv\Scripts\activate          # Windows

# Install dependencies
pip install -r requirements.txt

# Configure environment variables
cp .env.example .env
# Edit .env with your credentials (see Environment Variables below)

# Seed the database with sample data (optional but recommended)
python db/seed.py

# Start the server
uvicorn main:app --reload --port 8000
```

The API will be available at `http://localhost:8000`

### 4. Frontend Setup

```bash
cd frontend

# Install dependencies
npm install

# Configure environment variables
cp .env.example .env
# Edit .env if needed (default API URL: http://localhost:8000)

# Start the dev server
npm run dev
```

The app will be available at `http://localhost:5173`

---

## 🔐 Environment Variables

### Backend (`backend/.env`)

```env
DATABASE_URL=mysql+pymysql://root@localhost:3306/yelp_db
SECRET_KEY=your-jwt-secret-key
GOOGLE_API_KEY=your-google-ai-studio-api-key
TAVILY_API_KEY=your-tavily-api-key
UPLOAD_DIR=uploads
```

### Frontend (`frontend/.env`)

```env
VITE_API_URL=http://localhost:8000
```

> ⚠️ **Never commit `.env` files.** Only `.env.example` templates are tracked in Git.

---

## 📡 API Endpoints

Once the backend is running, full interactive documentation is available at:
- **Swagger UI:** [http://localhost:8000/docs](http://localhost:8000/docs)
- **ReDoc:** [http://localhost:8000/redoc](http://localhost:8000/redoc)

### Endpoint Summary

| Method | Endpoint | Auth | Description |
|---|---|---|---|
| `POST` | `/auth/signup` | ✗ | Register new user/owner |
| `POST` | `/auth/login` | ✗ | Login, returns JWT token |
| `GET` | `/users/me` | ✓ | Get current user profile |
| `PUT` | `/users/me` | ✓ | Update profile fields |
| `POST` | `/users/me/photo` | ✓ | Upload profile picture |
| `GET` | `/users/me/preferences` | ✓ | Get AI assistant preferences |
| `PUT` | `/users/me/preferences` | ✓ | Save AI assistant preferences |
| `GET` | `/users/me/history` | ✓ | User activity history |
| `GET` | `/restaurants/` | ✗ | List/search restaurants (paginated) |
| `GET` | `/restaurants/{id}` | ✗ | Get restaurant details |
| `POST` | `/restaurants/` | ✓ | Create restaurant listing |
| `PUT` | `/restaurants/{id}` | ✓ | Update restaurant (owner/creator) |
| `DELETE` | `/restaurants/{id}` | ✓ | Delete restaurant (owner/creator) |
| `POST` | `/restaurants/{id}/photos` | ✓ | Upload restaurant photo |
| `GET` | `/restaurants/{id}/reviews` | ✗ | List reviews for restaurant |
| `POST` | `/restaurants/{id}/reviews` | ✓ | Create review (1–5 stars + comment) |
| `PUT` | `/reviews/{id}` | ✓ | Update own review |
| `DELETE` | `/reviews/{id}` | ✓ | Delete own review |
| `GET` | `/favorites/` | ✓ | List favorite restaurants |
| `POST` | `/favorites/{id}` | ✓ | Add to favorites |
| `DELETE` | `/favorites/{id}` | ✓ | Remove from favorites |
| `POST` | `/ai-assistant/chat` | ✓ | AI chatbot conversation |

---

## 🤖 AI Assistant Chatbot

The AI chatbot provides personalized restaurant recommendations using:

- **Google Gemini** (via Langchain) for natural language understanding
- **Tavily Web Search** for real-time external context (hours, events, trending)
- **User Preferences** loaded from the database (cuisine, price, dietary, ambiance)
- **Multi-turn Conversations** with conversation history

### How It Works

1. User sends a message (e.g., *"I'm looking for a place for dinner tonight"*)
2. The system loads the user's saved preferences from MySQL
3. Langchain processes the query with a system prompt containing preferences + restaurant DB data
4. Tavily optionally enriches context with web search results
5. Gemini generates a personalized, conversational response with restaurant recommendations
6. Results are returned as structured JSON with clickable restaurant cards

### API Usage

```json
POST /ai-assistant/chat
{
  "message": "Something romantic for an anniversary",
  "conversation_history": []
}
```

---

## 🗄️ Database Schema

```
┌─────────────────┐     ┌──────────────────┐     ┌──────────────┐
│     users        │────▶│   restaurants     │────▶│   reviews    │
│                  │     │                   │     │              │
│  id              │     │  id               │     │  id          │
│  name            │     │  name             │     │  user_id  FK │
│  email (unique)  │     │  cuisine_type     │     │  rest_id  FK │
│  password_hash   │     │  description      │     │  rating 1–5  │
│  phone           │     │  address/city/zip │     │  comment     │
│  about_me        │     │  phone/hours      │     │  created_at  │
│  city/state/     │     │  price_tier $–$$$  │     │  updated_at  │
│  country         │     │  amenities (JSON) │     └──────────────┘
│  gender/language │     │  owner_id FK      │
│  profile_pic_url │     │  avg_rating       │     ┌──────────────┐
│  role            │     │  review_count     │     │   photos     │
│  created_at      │     │  view_count       │     │              │
└────────┬─────────┘     └──────────────────┘     │  id          │
         │                                         │  rest_id  FK │
         ▼                                         │  review_id FK│
┌─────────────────┐     ┌──────────────────┐      │  url         │
│ user_preferences │     │   favorites       │     └──────────────┘
│                  │     │                   │
│  user_id FK      │     │  user_id FK       │
│  cuisines (JSON) │     │  restaurant_id FK │
│  price_range     │     │  created_at       │
│  location        │     └──────────────────┘
│  preferred_locs  │
│  dietary (JSON)  │
│  ambiance (JSON) │
│  sort_preference │
└──────────────────┘
```

---

## 🧪 Demo Credentials (Seed Data)

After running `python db/seed.py`:

| Email | Password | Role |
|---|---|---|
| `prakhar@demo.com` | `password123` | User |
| `nikhil@demo.com` | `password123` | User |
| `owner@demo.com` | `password123` | Owner |

**Seeded Data:** 3 users · 6 restaurants · 7 reviews · 1 user preference set

---

## 🌿 Git Branch Strategy

| Branch | Purpose | Owner |
|---|---|---|
| `main` | Stable, reviewed code only | Both |
| `feature/backend-setup` | Backend APIs + DB + AI chatbot | Prakhar |
| `nikhil/frontend` | React UI + all pages | Nikhil |

> **Workflow:** Develop on feature branches → Review → Merge to `main`

---

## 📋 Non-Functional Requirements

- ✅ **Responsive Design** — Mobile, tablet, and desktop (TailwindCSS)
- ✅ **Accessibility** — Semantic HTML, alt text, keyboard navigation
- ✅ **Scalability** — Optimized queries, pagination, efficient API responses
- ✅ **Security** — bcrypt password hashing, JWT auth, input validation
- ✅ **Error Handling** — Proper HTTP status codes, descriptive error messages
- ✅ **Code Architecture** — Separation of concerns (`components/`, `pages/`, `services/`, `routes/`, `models/`)

---

## 📝 Submission Checklist

- [ ] Private repo with TAs (`Devdatta1999`, `Saurabh2504`) as collaborators
- [ ] Detailed commit messages throughout
- [ ] No `venv/`, `__pycache__/`, or `node_modules/` committed
- [ ] `requirements.txt` for Python dependencies
- [ ] `README.md` with setup instructions (this file)
- [ ] API documentation via Swagger (`/docs`)
- [ ] Lab report: `YourName_Lab1_Report.doc` uploaded to Canvas

---

## 📄 License

Private repository — Academic use only. San Jose State University.
